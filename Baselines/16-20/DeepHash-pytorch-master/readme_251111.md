**比较算法间性能时保持参数配置一致性**
        "resize_size": 224,
        "crop_size": 224,
        "batch_size": 64,
        "dataset": "cifar10-image","coco2017","ImageNet100

## 0108
align loss(v256=img; t256=img labels) + contrast loss(pos_logits_all) + prompt filtered captions(generated by chatgpt)
1. 新增改动放到csp_blip7
2. 修改filtered captions,现在是将数据初始captions喂给gpt后生成的，拿来直接用。
3. 求t256的逻辑变为根据该目标图像的multi hot转换而来的带有prompt的多标签的句子的向量，而不是之前的从captions_bank负样本对取
4. captions_bank, prompt_bank最终只用做计算contrast loss
5. 改为训练前预计算t256_cache，并释放text_encoder缓存
6. 增加最后保存机制 final loss, km, , best map stored in save/save_path/....jsonl
 
``
## 1224
multi-positive InfoNCE  
1. 修改对比函数Loss计算，不再用 pos_vecs = prompt_bank[y_idx] 得到单个正样本，而是对每个样本用 labels>0 得到 pos_mask（多正类集合），
计算 pos_logits_all = img_feats @ prompt_bank.T / tau（[B, C]）  
3. 分子：对每个样本在其所有正类上做 logsumexp；分母：对“所有 prompt + 负 caption”做 logsumexp  
4. 负样本屏蔽从“屏蔽 y_idx 对应类”改为“屏蔽该样本所有正类对应的 caption”：mask = pos_mask[:, neg_ids]  
2. filtered captions用LLM来做，但是要设计好提示词：比如：参考TFIDF算法，词的重要程度或者用自然语言：  这是一组关键词，用一句话来描述这十组关键词，应该优先包含出现频率高的词，且其中最重要的是Label这个词；形成一句话或者a photo of ..and ...and ...
3. todo:1. 对齐loss，多标签情况下，直接用a photo of 这几个标签 拼成一句话，用这句话的特征向量和v256q去做MSE loss
2. 对比loss, 目的是让t256维度的分布和csq哈达玛的分布尽可能的小，方法是：不直接用label的特征去做分子和分母，而是用LLM后的那句话做分子分母

## 1210
1. make_prompt_for_captions用于 对于同一类的K条captions去重优化为一组关键词，表示该类的关键信息，每类用一个特征表示  
2. 

## 1116
1. trainset 5w时相比1w map大幅提高

## 1115
1. eyBERT 强制 CPU，不再占 GPU 约省1G显存
2. TextEncoder用完后, 清理模型显存  约省14G显存

## 1114
1. 修复label_to_text，支持多词作为classname
2. 修复precompute_class_captions，新增dataloader从train_all/txt中读，解决captions内容不完整的情况
3. 调整打印格式


**运行方法**
1. `optimizeAccel.py`用于单独测试新数据集的hashcenter
2. 


**评价指标**
查看输出csv中map的值，此map是通过计算得来的最终map


**其他注意事项**
算法模型间比较的前提是通过
trn_binary.npy	数据库图像的哈希码
trn_label.npy	数据库图像的标签
tst_binary.npy	查询图像的哈希码
tst_label.npy	查询图像的标签
的计算


`clean_save_dir_keep_best`来清理 save 目录中除最佳 mAP 文件外的所有模型/中间文件  

因Blip模型"resize_size"已经预设为224,为公平比较,需将被比较算法设置"resize_size": 224  